# GitHub Pages 自動部署說明

## 📋 概述

本專案已配置 GitHub Actions 自動部署流程，當推送到 `dev/v1.2` 分支時，會自動建置並部署到 GitHub Pages。

## 🚀 自動部署流程

### 觸發條件

1. **自動觸發**：當推送到 `dev/v1.2` 分支時
2. **手動觸發**：在 GitHub Actions 頁面可以手動執行工作流程

### 部署步驟

工作流程會執行以下步驟：

1. ✅ **Checkout** - 檢出程式碼
2. ✅ **Setup Node.js** - 設定 Node.js 20 環境
3. ✅ **Install dependencies** - 安裝依賴套件（使用 `npm ci`）
4. ✅ **Build** - 執行 `npm run build` 建置專案
5. ✅ **Setup Pages** - 設定 GitHub Pages
6. ✅ **Upload artifact** - 上傳建置產物
7. ✅ **Deploy** - 部署到 GitHub Pages

## ⚙️ GitHub Pages 設定步驟

### 第一次設定（僅需執行一次）

1. 前往 GitHub 儲存庫的 **Settings** 頁面
2. 在左側選單中找到 **Pages** 選項
3. 在 **Source** 區塊中：
   - 選擇 **GitHub Actions** 作為來源
   - 不需要選擇分支，因為工作流程會自動處理
4. 點擊 **Save** 儲存設定

### 驗證設定

設定完成後，您應該會看到：
- **Source**: GitHub Actions
- **Workflow**: Deploy to GitHub Pages
- **Status**: 顯示部署狀態

## 📝 工作流程檔案位置

工作流程檔案位於：`.github/workflows/deploy-pages.yml`

### 工作流程配置說明

```yaml
on:
  push:
    branches:
      - dev/v1.2  # 只監聽 dev/v1.2 分支
  workflow_dispatch: # 允許手動觸發

permissions:
  contents: read    # 讀取儲存庫內容
  pages: write      # 寫入 GitHub Pages
  id-token: write   # 用於 OIDC 認證
```

## 🔍 檢查部署狀態

### 方式 1：GitHub Actions 頁面

1. 前往儲存庫的 **Actions** 標籤
2. 點擊 **Deploy to GitHub Pages** 工作流程
3. 查看最新的執行記錄

### 方式 2：GitHub Pages 設定頁面

1. 前往 **Settings** > **Pages**
2. 查看部署狀態和 URL

## 🌐 訪問部署的網站

部署成功後，網站將可在以下 URL 訪問：

```
https://[您的GitHub使用者名稱].github.io/[儲存庫名稱]/
```

例如：
```
https://mkhsu2002.github.io/AI-EC-SEO-Booster/
```

## 🛠️ 建置配置

### Vite 配置

專案使用相對路徑 `base: './'`，確保在 GitHub Pages 子路徑下也能正常運作。

### 建置輸出

- **輸出目錄**: `dist/`
- **建置命令**: `npm run build`

## ⚠️ 注意事項

1. **分支限制**：目前只監聽 `dev/v1.2` 分支的推送
2. **Node.js 版本**：使用 Node.js 20
3. **建置時間**：首次部署可能需要幾分鐘時間
4. **快取**：npm 套件會自動快取，加速後續建置

## 🔄 更新部署

### 自動更新

當您推送程式碼到 `dev/v1.2` 分支時，部署會自動觸發：

```bash
git add .
git commit -m "feat: 更新功能"
git push origin dev/v1.2
```

### 手動觸發

1. 前往 **Actions** 標籤
2. 選擇 **Deploy to GitHub Pages** 工作流程
3. 點擊 **Run workflow** 按鈕
4. 選擇分支並執行

## 🐛 故障排除

### 部署失敗

1. 檢查 **Actions** 頁面的錯誤訊息
2. 確認 `package.json` 中的建置腳本正確
3. 確認 `vite.config.ts` 配置正確
4. 檢查 Node.js 版本是否為 20

### 網站無法訪問

1. 確認 GitHub Pages 設定為 **GitHub Actions**
2. 檢查工作流程是否成功執行
3. 確認建置產物是否正確上傳
4. 等待幾分鐘讓 DNS 更新

### 資源載入失敗

1. 確認 `vite.config.ts` 中 `base: './'` 設定正確
2. 檢查 `dist/` 目錄中的檔案是否正確
3. 檢查瀏覽器控制台的錯誤訊息

## 📚 相關文件

- [GitHub Pages 文件](https://docs.github.com/en/pages)
- [GitHub Actions 文件](https://docs.github.com/en/actions)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

---

**最後更新**: 2024年12月

